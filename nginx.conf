events {
	worker_connections 1024;
}
http {
	include /etc/nginx/mime.types;
	sendfile on;
	server {
		listen 80;
		root /usr/share/nginx/html/;
		location / {
			try_files index.html /index.html = 404;
		}
		location ~/assets/\.(gif|png|jpg)$ {

		}
		location /public {

		}

	}
}
